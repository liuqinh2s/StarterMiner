你是一个数据合并专家。请将以下两组初创公司数据合并：
1. 已有数据（历史积累）
2. 新数据（今日抓取）

合并规则：
- 相同公司（id 相同或名称相似）合并，保留最新信息，评分取加权平均（历史 0.7 + 新 0.3）
- 新公司直接加入
- 讨论数据全部保留，去重（相同 URL）
- 返回合并后的完整 JSON

已有数据：
{existing}

新数据：
{new_data}

返回纯 JSON，格式同输入。
